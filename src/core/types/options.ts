export interface LoginServerConfigureOptions {
    host: string;
    port: number;
    //protocolVersion?: number;
}

export interface LoginServerEnterOptions {
    login: string;
    password: string;
}
